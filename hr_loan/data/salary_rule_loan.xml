<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--Adding a salary rule for loan-->
    <data noupdate="1">
        <record id="hr_rule_loan" model="hr.salary.rule">
            <field name="code">LO</field>
            <field name="name">Emprestimo</field>
            <field name="category_id" ref="l10n_ao_hr_payroll.hr_salary_rule_category_deducoes"/>
            <field name="struct_id" ref="l10n_ao_hr_payroll.hr_salary_structure_base"/>
            <field name="amount_select">code</field>
            <field name="condition_select">python</field>
            <field name="condition_python">res=False
for rem in contract.remuneration_ids:
    if rem.remunerationcode_id.code == &quot;des_emprestimo&quot; and rem.date_start &lt;= payslip.date_to:
        if not rem.date_end or rem.date_end &gt;= payslip.date_to:
            res=True
result = res
</field>
            <field name="amount_python_compute">res=0.0
for rem in contract.remuneration_ids:
    if rem.remunerationcode_id.code == &quot;des_emprestimo&quot; and rem.date_start &lt;= payslip.date_to:
        if not rem.date_end or rem.date_end &gt;= payslip.date_to:
            res += rem.amount
result = res * -1
</field>
            <field name="appears_on_payslip" eval="True"/>
            <field name="sequence" eval="190"/>
        </record>

        <record id="hr_rule_loan" model="hr.salary.rule">
            <field name="code">SAR</field>
            <field name="name">Adiantamento Salario</field>
            <field name="category_id" ref="l10n_ao_hr_payroll.hr_salary_rule_category_deducoes"/>
            <field name="struct_id" ref="l10n_ao_hr_payroll.hr_salary_structure_base"/>
            <field name="amount_select">code</field>
            <field name="condition_select">python</field>
            <field name="condition_python">res=False
for rem in contract.remuneration_ids:
    if rem.remunerationcode_id.code == &quot;des_adia_salario&quot; and rem.date_start &lt;= payslip.date_to:
        if not rem.date_end or rem.date_end &gt;= payslip.date_to:
            res=True
result = res
</field>
            <field name="amount_python_compute">res=0.0
for rem in contract.remuneration_ids:
    if rem.remunerationcode_id.code == &quot;des_adia_salario&quot; and rem.date_start &lt;= payslip.date_to:
        if not rem.date_end or rem.date_end &gt;= payslip.date_to:
            res += rem.amount
result = res * -1
</field>
            <field name="appears_on_payslip" eval="True"/>
            <field name="sequence" eval="191"/>
        </record>


    </data>
    <data noupdate="1">

    <record id="hr_remuneration_code_descontoemprestimo" model="hr.remuneration.code">
        <field name="code">des_emprestimo</field>
        <field name="name">Emprestimo</field>
        <field name="type">deduction</field>
    </record>

    <record id="hr_remuneration_code_desconto_adia_salario" model="hr.remuneration.code">
        <field name="code">des_adia_salario</field>
        <field name="name">Desconto Adiantamento Salario</field>
        <field name="type">deduction</field>
    </record>

    </data>
</odoo>
